# Ключи и сертификаты не коммитить
ssl/*.pem
ssl/*.key
ssl/*.crt

# DB backups
backups/

# Env files (секреты)
.env
.env.*

# Docker Compose overrides (секреты на сервере)
docker-compose.override.yml
